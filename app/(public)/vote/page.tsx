import { ComingSoonPage } from "@/components/dashboard/coming-soon-page";

export default function VotePage() {
  return (
    <ComingSoonPage
      title="Vote Rewards"
      description="Voting, streak rewards, and vote leaderboards are planned, but the vote provider is not hooked into this public release yet."
      integration="vote provider API"
    />
  );
}
