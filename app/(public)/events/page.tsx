import { ComingSoonPage } from "@/components/dashboard/coming-soon-page";

export default function EventsPage() {
  return (
    <ComingSoonPage
      title="Civilization Events"
      description="Server-wide events are planned for the next gameplay plugin release. For now, event-like news belongs in announcements and Discord."
      integration="ZNationsEvents plugin"
    />
  );
}
